import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#58a6ff',
        },
        secondary: {
            main: '#fff',
        },
        background: {
            default: '#0d1117'
        }
    }
});

export default theme;